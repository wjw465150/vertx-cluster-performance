rootProject.name = 'vertx-cluster-performance'
if (!JavaVersion.current().java8Compatible) {
  throw new IllegalStateException('''A Vert.x App:
                                    |  This needs Java 8,
                                    |  You are using something else,
                                    |  Refresh. Try again.'''.stripMargin())
}
